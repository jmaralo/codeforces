#include <bits/stdc++.h>

using namespace std;

#define TESTCASES false
void solve(int tt) {
    int x, y;
    cin >> x >> y;
}

int main() {
    if (!TESTCASES) {
        solve(0);
        return 0;
    }

    int t;
    cin >> t;
    for (int i = 0; i < t; i++) {
        solve(i + 1);
    }
    return 0;
}