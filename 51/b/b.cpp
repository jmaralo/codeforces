#include <bits/stdc++.h>

using namespace std;

#define TESTCASES true
void solve(int tt) {
    // <-- Solution goes here -->//
}

int main() {
    if (!TESTCASES) {
        solve(0);
        return 0;
    }

    int t;
    cin >> t;
    for (int i = 0; i < t; i++) {
        solve(i + 1);
    }
    return 0;
}